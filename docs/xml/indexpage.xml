<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.9.1" xml:lang="en-US">
  <compounddef id="indexpage" kind="page">
    <compoundname>index</compoundname>
    <title>Perfect hash functions</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><anchor id="index_1md_README"/></para>
<para>Perfect hash functions may be used for memory efficient storage and fast retrieval of items from static sets, among other uses.</para>
<sect1 id="index_1autotoc_md1">
<title>Description</title>
<para>We implement two data structures that implement the abstract data type of perfect hash functions.</para>
<para>We denote them respectively by <computeroutput>phf</computeroutput> and <computeroutput>phf_lvl2</computeroutput>. For small sets, <computeroutput>phf</computeroutput> is more memory efficient and provides faster queries. However, construction can be very slow for larger sets. In this case, <computeroutput>phf_lvl2</computeroutput> should provide much faster constructions, at the cost of slightly slower look-up times and more in-place memory requirements. However, it is still quite fast and memory-efficient.</para>
</sect1>
<sect1 id="index_1autotoc_md2">
<title>Memory efficiency</title>
<para>More gains in memory efficiency are readily possible by using in-place compression, e.g., in <computeroutput>phf</computeroutput>, the <computeroutput>l</computeroutput> value can be easily compressed since it should be geometric with a known <computeroutput>p</computeroutput> value. In this case, geometric coding may be used. However, since we are only storing a single value, there does not seem to be much point in doing so.</para>
<para>It makes more sense to pursue in-place compression for <computeroutput>phf_lvl2</computeroutput>, which stores a vector of integers. </para>
</sect1>
    </detaileddescription>
    <location file="README.md"/>
  </compounddef>
</doxygen>
